# PM 运行报告 · 2026-W35

---
### run-2026w35-paradigm-shift · 2026-08-25 · GLM-5.3 首位 PM 会话（owner 授权直执行）
scope: [IR-0005 #348, #349, #350, #351, ADR-0085, CIW#96/#97, archive#17, .github#352/#353/#354/#355, cnb-bridge#1]

**事实**

- PM 优先范式全量落地（ADR-0085）：四道门禁制度化（T7 卡就绪/T8 卡完成/T9 IR 验收
  +conductor 谓词）、AGENTS.md 重写为 PM 手册（协议块字节不变）、PLAYBOOK/providers
  目录/ghcb 四命令（board/dispatch/accept/report）、EX-1 三接缝（GOVERNANCE 条目 +
  cnb-dispatch/cnb-audit 工作流 + org secrets 双 key）。
- 退役执行：agent-registry/agent-platform/agent-tools 三仓 GitHub archived；声明层
  87 文件快照入 archive/retired/；ADR 家园单仓化（INDEX v2 + 83 条目，gate/org-gate/
  drift-check §10 三处改址）；REPOS/languages/contracts/expected-state 联动清理。
- CNB 实弹全链：账号 p11 → 窗口 #207 → NPC 13s 真实执行（echo/uname/python3 -V，
  退出码 0）→ 回复含 [run:edd02570] 锚串 → collect 回收成功。配额实测：p11 满额
  1600 核时（{dev_in_sec:{total,free}} 嵌套结构）；xuemei token 有效但对窗口仓 404
  （pending-access，需 owner 在 CNB 侧加组授权）。
- 冷上下文六问复测：a-f 全答、主链路零假链（对比转变前的八卡点摩擦报告）；遗留
  断链 2 处（SECURITY.md/sli-report.sh 指退役仓）已随收尾 PR 修复。
- 状态机实走：T3 认领 ×3 ✓；T8 首走暴露两个真 bug 并修复（双态窗口 abort 致标签
  转移全族不可达→收敛语义；谓词本仓清单法漏跨仓绑定→search API 主通道）；T8 正向/
  负向（fail-closed 拒绝回退）双向证据齐。
- 过程事故 3 起，全部当场修复：①Windows 下 git stash pop 在 autocrlf 引入 CRLF 致
  CI eval 失败（重归一+全树行尾审计）；②org-gate jq capture 无名组报错被吞=幽灵
  ADR 误判（潜伏缺陷，改名组修复）；③github.com 443 不可达期间全量改走 Git Data
  API 推送（api_push 工具），PR#354 因分支基线漂移成空 diff（compare 验证后重建）。

**体感**（主观）

- 状态机的"标签写序"是隐形契约——transitions.yaml 注释里其实写了（bugflow 先移旧
  再置新），但 T1-T9 主流程没有明示，我作为 PM 是踩了才知道。playbook 应有一节
  "状态写序口诀"。
- 空仓 bootstrap 的平台约束（Git Data API 409/contents API 可通/ruleset 无 bypass）
  散落在 expected-state 注释的豁免先例里，没有 runbook 化——第二次做还要考古。
- cnb_pool 的 comments 分页假设（单页够用）在真实窗口（数百条历史评论）上直接翻车；
  "实测三连"（配额嵌套结构/窗口池被清空/分页）说明 CNB 侧现实与文档指南有漂移，
  指南应标注"以实测为准"。
- api_push（API 推送通道）应早建——网络故障消耗的调试时间比写它多。
- conductor 谓词的跨仓绑定（卡在治理仓、PR 落任意仓）是设计期没想清楚的现实——
  "Card: 元数据"协议本身就跨仓，谓词第一版却按本仓写。

**改进点**

- [followup] playbook: PLAYBOOK 增补"状态标签写序口诀"（置新态=直接加新标签，conductor 双态窗口收敛语义；勿手动删旧态）与"空仓 bootstrap runbook"（contents API 首commit+豁免登记两步）
- [followup] tool: ghcb dispatch 增加 --wait（同步等待 NPC 回复并本地校验锚串，替代手工 collect 两步）
- [followup] policy: IR-0004 spec rev6（CNB 默认主力/ASSUMPTION-03→DECISION/AC-10 执行者语义）——owner 醒后第一张小卡
- [followup] gate: conductor T8 的 search 索引延迟（分钟级）意味着刚合并就置 done 可能误拒——建议 PLAYBOOK 注明"合并后等 2 分钟再置 done"或谓词加 30s 重试
- [followup] tool: PM 工作区配 jq（test-ir0002 等本地自测依赖；CI 有本地无）
- [followup] policy: CNB 窗口池治理（窗口回收/关闭策略/僵尸窗口清理）待实践沉淀后成文
