# PM 运行报告 · 2026-W35

---
### run-2026w35-paradigm-shift · 2026-08-25 · GLM-5.3 首位 PM 会话（owner 授权直执行）
scope: [IR-0005 #348, #349, #350, #351, ADR-0085, CIW#96/#97, archive#17, .github#352/#353/#354/#355, cnb-bridge#1]

**事实**

- PM 优先范式全量落地（ADR-0085）：四道门禁制度化（T7 卡就绪/T8 卡完成/T9 IR 验收
  +conductor 谓词）、AGENTS.md 重写为 PM 手册（协议块字节不变）、PLAYBOOK/providers
  目录/ghcb 四命令（board/dispatch/accept/report）、EX-1 三接缝（GOVERNANCE 条目 +
  cnb-dispatch/cnb-audit 工作流 + org secrets 双 key）。
- 退役执行：agent-registry/agent-platform/agent-tools 三仓 GitHub archived；声明层
  87 文件快照入 archive/retired/；ADR 家园单仓化（INDEX v2 + 83 条目，gate/org-gate/
  drift-check §10 三处改址）；REPOS/languages/contracts/expected-state 联动清理。
- CNB 实弹全链：账号 p11 → 窗口 #207 → NPC 13s 真实执行（echo/uname/python3 -V，
  退出码 0）→ 回复含 [run:edd02570] 锚串 → collect 回收成功。配额实测：p11 满额
  1600 核时（{dev_in_sec:{total,free}} 嵌套结构）；xuemei token 有效但对窗口仓 404
  （pending-access，需 owner 在 CNB 侧加组授权）。
- 冷上下文六问复测：a-f 全答、主链路零假链（对比转变前的八卡点摩擦报告）；遗留
  断链 2 处（SECURITY.md/sli-report.sh 指退役仓）已随收尾 PR 修复。
- 状态机实走：T3 认领 ×3 ✓；T8 首走暴露两个真 bug 并修复（双态窗口 abort 致标签
  转移全族不可达→收敛语义；谓词本仓清单法漏跨仓绑定→search API 主通道）；T8 正向/
  负向（fail-closed 拒绝回退）双向证据齐。
- 过程事故 3 起，全部当场修复：①Windows 下 git stash pop 在 autocrlf 引入 CRLF 致
  CI eval 失败（重归一+全树行尾审计）；②org-gate jq capture 无名组报错被吞=幽灵
  ADR 误判（潜伏缺陷，改名组修复）；③github.com 443 不可达期间全量改走 Git Data
  API 推送（api_push 工具），PR#354 因分支基线漂移成空 diff（compare 验证后重建）。

**体感**（主观）

- 状态机的"标签写序"是隐形契约——transitions.yaml 注释里其实写了（bugflow 先移旧
  再置新），但 T1-T9 主流程没有明示，我作为 PM 是踩了才知道。playbook 应有一节
  "状态写序口诀"。
- 空仓 bootstrap 的平台约束（Git Data API 409/contents API 可通/ruleset 无 bypass）
  散落在 expected-state 注释的豁免先例里，没有 runbook 化——第二次做还要考古。
- cnb_pool 的 comments 分页假设（单页够用）在真实窗口（数百条历史评论）上直接翻车；
  "实测三连"（配额嵌套结构/窗口池被清空/分页）说明 CNB 侧现实与文档指南有漂移，
  指南应标注"以实测为准"。
- api_push（API 推送通道）应早建——网络故障消耗的调试时间比写它多。
- conductor 谓词的跨仓绑定（卡在治理仓、PR 落任意仓）是设计期没想清楚的现实——
  "Card: 元数据"协议本身就跨仓，谓词第一版却按本仓写。

**改进点**

- [followup] playbook: PLAYBOOK 增补"状态标签写序口诀"（置新态=直接加新标签，conductor 双态窗口收敛语义；勿手动删旧态）与"空仓 bootstrap runbook"（contents API 首commit+豁免登记两步）
- [followup] tool: ghcb dispatch 增加 --wait（同步等待 NPC 回复并本地校验锚串，替代手工 collect 两步）
- [followup] policy: IR-0004 spec rev6（CNB 默认主力/ASSUMPTION-03→DECISION/AC-10 执行者语义）——owner 醒后第一张小卡
- [followup] gate: conductor T8 的 search 索引延迟（分钟级）意味着刚合并就置 done 可能误拒——建议 PLAYBOOK 注明"合并后等 2 分钟再置 done"或谓词加 30s 重试
- [followup] tool: PM 工作区配 jq（test-ir0002 等本地自测依赖；CI 有本地无）
- [followup] policy: CNB 窗口池治理（窗口回收/关闭策略/僵尸窗口清理）待实践沉淀后成文

---
### run-2026w35-ir004-rev6 · 2026-08-25 · GLM-5.3 PM 会话（第二段：IR-0004 rev6 全量交付）
scope: [IR-0004 #315, #316-#335, #358, ADR-0086, CIW#98-#105, cnb-bridge#2, archive#19, .github#359/#360]

**事实**

- spec rev6（owner 三原则：fan-out 仪器化/oracle 接口化/CNBC 默认主力）合并（#359）；suite 指纹同步。
- 五路子代理并行构建（A1 变异+属性裁判/A2 模糊蜕变符号 SAST 形式化/B DSL+熵/C oracle+燃料+演练/D CNB 底座收尾），合计 201 离线自测全绿；集成经 8 个 PR 合并（CIW#100/102/103/104/105、cnb-bridge#2、archive#19、.github#360）。
- 运行时证据真跑：DSL 编译→校验→篡改红全链（真实 spec）；oracle 硬区 exit2；静态干跑双真仓 green；#334 试点链（三骨架→熵 20% 收敛→哈希链产物→消费 exit 0）。
- 24 张卡（20 波次+3 范式+1 rev6）全部 T7→T3→T8 走完 state:done——状态机批量流转首次全量实走。
- adversary 跨仓目标支持（CIW#98）+ model 覆盖旋钮（#99）——但 LLM 通道 infra 故障（kimi-for-coding 与 glm-4.5-air 对大请求均 2xx 异常体；小探针正常）三次实弹挡住红队幸存记录，rev6 与 #360 按授权 bypass 合并并留痕。

**体感**（主观）

- 子代理产物质量高但集成缝真实存在：B/C 两代理的哈希链约定不一致（试点实跑才暴露）；A2 幻觉了三个不存在的仓名（拿 REPOS.yaml 勘误）——多代理开发的集成审查必须对照 SSOT，这条值得进 PLAYBOOK。
- zizmor artipacked 在子代理 workflow 上反复出现 7 次（裸块/with 块/第三处）——工作流模板应预置 persist-credentials。
- GitHub 搜索索引对"合并后编辑的 PR 正文"收录极慢，跨仓卡绑定的 T8 谓词因此空查——用本仓补锚 PR 解决，但更根本的修法是 T8 增加指定绑定仓直查参数。
- CRLF/字节码缓存/转义三层"环境鬼"消耗了约一小时——Windows 工作区的 PM 应配 WSL 或至少 core.autocrlf=false。

**改进点**

- [followup] playbook: 集成审查清单增补"子代理产物必须对照 SSOT 核对实体名（仓名/字段名）"与"工作流模板预置 persist-credentials"
- [followup] gate: T8 谓词增加 --binding-repo 直查通道（搜索索引延迟的根治）；adversary 通道修复后对 #359/#360 补红队
- [followup] tool: mutation-weekly 对 AI_Web_School 首跑建议 owner 在场触发；X-04 条款文本修订（triggered）待小 PR+ADR
- [followup] policy: LLM 通道供应商切换决策（kimi coding endpoint 异常体问题）——org var MODEL1 或 LLM_ENDPOINT1 择一改

---
### run-2026w35-nav363 · 2026-08-25 · GLM-5.3 worker 会话（#362 审计导航收口）
scope: [#362, #363, .github#364]

**事实**

- #362 治理审计（32 次 PM 模拟，置信度 4.8/10）的导航断裂按 bug 流收口：bug 单 #363（bug.yml 表单契约）→ bugflow 三值判定 `reproduced`（bugfp:72ba901d…，run 32820512840）→ 修复 PR .github#364（docs/NAVIGATION.md 全入口路由表 + AGENTS/PLAYBOOK/profile/Makefile 规则落点 + governance/tests/test-navigation.sh 35 项断链检测入 gate）→ 全机器关卡绿（gate/org-gate/adversary/t14/hygiene/adr-required）。
- 首次 reproduce 判定 inconclusive：复现用例 fail 分支未带 exit 1（bugflow 契约=fail 判定要求 marker=fail 且 rc≠0），勘误后 reproduced。
- 首次派发后 issue body 被管道事故清空（sed 分隔符与 shell || 冲突），已恢复完整表单并留 erratum 评论。

**体感**（主观）

- bug 流跨仓触发面有暗坑：issue_comment(/reproduce) 只对 CI-Workflows 本仓 issue 生效，.github 仓的 bug 单必须 workflow_dispatch 派发——bug.yml 表单与 NAVIGATION 均未说明，陌生 agent 会卡住。
- bugflow 的 fail 语义（marker 且 rc≠0）对文档型复现用例不直观，本 run 实测踩中。

**改进点**

- [followup] playbook: bug 流跨仓派发说明补入（/reproduce 仅 CI-Workflows 本仓生效；跨仓 bug 单用 workflow_dispatch bug-reproduce.yml 带 issue_repo/issue_number）
- [followup] tool: bug.yml 复现用例字段说明补退出码契约示例（fail 判定须 rc≠0，附本 run 反例）
- [followup] policy: #362 P1/P2 未收口项（PM persona 边界成文、CI-Workflows Makefile、org-gate dry-run、CNB ledger 一词多义等）待 owner 分卡处置

---
### run-2026w35-bypass-baseline · 2026-08-25 · GLM-5.3 PM 会话（ADR-0090 bypass 基线 + 漂移误报清零）
scope: [ADR-0090, ADR-0083 补全, .github#369/#370, archive#25, agent-registry 运维]

**事实**

- owner 直接指令"所有 ruleset 必须 org admin 可 bypass + 建立飘移监控基线"落地：
  org-required-workflows 补 OrganizationAdmin/always（PR#369），其余三 ruleset
  已然如此；漂移基线=drift-check §1 不变量（落盘声明缺 admin/always bypass 即红，
  防"改文件+apply 两侧同摘"侵蚀路径）；ADR-0090 正本入 archive（#25）。
- PR#370 消两处 ADR-0083 关联改版遗留的每日误报：① §15 钉点仍按 tag 解引用，
  refs/tags/main 恒 404（且 ref_commit 已删、jq 裸取得 "null"）——改 sha/branch/tag
  三形态解引用 + 审判源文件存在性后验（workflows[] 每个文件在钉点 ref 上真实
  存在，文件被删=required workflow 静默失效即时检出）；② §12 adversary 08-24
  上线，旧分类器只认 org-gate 生效时刻——泛化 CHECK_EFFECTIVE 锚点表，
  生效前完结 CI 的 head 归 (a) 待接入（test-ir0002 5/5，(b) 检出不降级）。
- 真漂移一项收口：agent-registry PR#88 auto-merge 54h 卡死（§13）——退役归档仓
  死信，GraphQL unarchiveRepository → 关闭 #88/#78（内容已迁移 archive
  ADR-0076/0055）→ archiveRepository 复档，终态仍 archived。
- 终态机械验证：全量 drift-check 132 OK / 0 DRIFT / exit 0（§15 输出
  branch main → e5f19263 + 2 审判源在册；§12 待接入 10 项计数可见）。

**体感**（主观）

- ADR-0083 改版时只改了声明侧（expected-state/rulesets），检测器侧没跟——
  "决策文档写了内容完整性改由后验承载"，但后验代码从未落地；决策与执法面的
  改动应同 PR 交付，否则误报会在下一次 schedule 时才暴露。
- REST 的 archive/unarchive 端点对 owner PAT 恒 404（权限模型疑云），GraphQL
  mutation 一发即中——归档类运维走 GraphQL 是正道，值得 runbook 化。
- 运行中的 bash 脚本不能边跑边改（增量读取，字节位移会撕裂执行）——本轮两次
  中断重跑换来的教训；改 drift-check 前先确认没有在跑的实例。

**改进点**

- [followup] playbook: 归档仓死信 PR 清理 runbook（GraphQL unarchive→close→archive 三步 + REST 404 实测记录）
- [followup] tool: drift-check §15 锚点表 CHECK_EFFECTIVE 与 expected-state 联动（锚点写进声明文件而非脚本硬编码，新增 required check 不用改脚本）
- [followup] policy: ADR-0083 关联改版的"检测器跟进"缺口补记（决策文档承诺的后验与本轮落地件互引）

---
### run-2026w36-366-batch2 · 2026-08-25 · GLM-5.3 PM 会话（#366 治理审计 v2 第二批收口）
scope: [#366, .github#371, CIW#113/#115, governance-drift 分通道, CIW main 两红]

**事实**

- #366 高频阻断五项全清：孤儿 transitions.yaml 删除（路径统一 governance/）；
  drift 只读替代命令（PLAYBOOK/NAVIGATION，含 failure 语义解读）；persona 边界
  块；CIW Makefile gates-pr；contract gate 前批已修（CIW#113）。PR：.github#371
  + CIW#115，全关卡绿后按序合并（CIW 先——#371 文档声称"CIW 同款目标已就位"）。
- CIW main 两红（30+ 小时）收口：adversary-selftest T1/T5 硬编码模型名两代过期
  （kimi-for-coding→sensenova→deepseek-v4-flash，ADR-0088 切换事件）——改从
  models.yaml 动态派生 WANT_MODEL；gitleaks 对 Actions secret 名字引用误报——
  .gitleaksignore 指纹登记。合并后 CIW CI 全绿。
- governance-drift.yml 分通道补全：infra 通道此前只分 TITLE——BODY 仍"发现漂移
  …apply.sh"、指纹=sha256(空) 撞历史后静默跳过（GOVERNANCE_TOKEN 失效 30+h 仅
  log 可见，03:41 误导评论实证）——独立指纹（infra: 前缀+FATAL 归一化）+独立
  BODY（指向凭据轮换）。
- P0 不可达项上报：GOVERNANCE_TOKEN 失效（08-24 05:32 起）致每小时治理工作流
  全部失明——#344 已登记，处置=owner 轮换 org secret。

**体感**（主观）

- 测试断言硬编码"当前值"是反复腐烂源：T1/T5 各硬编码了一代模型名，每次合法
  切换破一次 CI。断言语义若是"与真源一致"，就该运行时读真源——写死等于把
  真源复制了一份注定过期的副本。
- .gitleaksignore 的注释里不能写触发字符串的字面量——注释本身进 git 历史、
  被 gitleaks 全历史扫描，自引爆（本 run 实测：ignore 文件自己成了唯一 finding，
  amend 强推才清掉）。登记误报要描述特征而非引用原文。
- "分通道"类设计要通道全覆盖：只分标题不分正文/指纹=半成品——去重机制会把
  故障报告吞成静默跳过，比不分通道更隐蔽。

**改进点**

- [followup] policy: GOVERNANCE_TOKEN 失效的 hour 级可见性（每小时 run 红 30+h 无人知）——建议 drift infra 通道开 issue 后 @owner 加急标记，或 deadman 类告警接入
- [followup] tool: CIW 测试面模型名断言全面排查（metering/bugflow 等是否还有硬编码当前值——本 run 只修了撞红的 adversary 两处）
- [followup] playbook: gitleaks 误报登记规范（注释禁含触发字面量；描述特征+指纹）补入 C1 runbook 或 CIW README

---
### run-2026w35-govtoken-validation · 2026-08-25 · GOVERNANCE_TOKEN 轮换后全量验证（owner 面）
scope: [.github#372(开+关), .github#373, #344(新指纹评论), run 32852115897 等 11 个 dispatch]

**事实**

- owner 按三 scope 规格（repo/admin:org/write:project，经典 PAT）轮换 org secret
  `GOVERNANCE_TOKEN` 后，dispatch 全量消费方实测：governance-drift / cost-check /
  auto-fix-limit / butler-reconcile / flaky-sweep / holdout-canary-sweep /
  butler-heartbeat-watch / board-sync / butler-ledger / seed-drill /
  butler-deadman-trip(simulate) 共 11 个。
- 结果：8 绿；3 红**均为设计内信号**——governance-drift=真漂移 4 条（Viral_Radar
  未申报 REPOS.yaml + 破玻璃直推未回填×3：archive be94906f/c1d31e10、Viral_Radar
  db02d592）；holdout-canary-sweep=正控缺失 P1（窗口内无演习，fail-closed）→
  drill 注入正控后联动 sweep 复核绿；deadman-trip=已熔断可见信号。
- #371 分通道修复在真实"故障→恢复"切换中验证：12:34 infra 通道评论（token 失效期
  最后一次）→ 13:21 真漂移通道评论，同 issue #344 两通道指纹不相交，无互撞。
- deadman-trip simulate=true 仍真实置位 AUTO_MERGE_DISABLED（SIM 只改审计标注）——
  复位+读回 false，#372 开立后评论+关闭（演习性质留痕）。
- **发现存量 bug**：seed-drill 台账步骤 git identity 配在工作区 local config，
  `git -C ledger commit` 读不到 → Author identity unknown → push 永不发生 →
  drill-ledger 分支远端从未存在（404 实证），台账记录随 runner 销毁丢失
  （append-only 无声违反，仅 run 变红兜底）。修法=--global（两处同款），PR .github#373
  全关卡绿待 owner review；合并后需 re-dispatch seed-drill 验证分支创建+落盘。

**体感**

- `simulate` 不等于 dry-run：deadman-trip 的演习语义是"测真实 trip 路径"，副作用
  真实发生。跑演习类 workflow 前必须读实现确认 SIM 边界，否则误开熔断。
- fail-closed 体系在凭据轮换场景的端到端表现：失明期不误报漂移（infra 通道）、
  恢复后第一条 run 即浮出存量漂移（Viral_Radar 未申报正是失明窗口内新建的仓）——
  检测器盲区=漂移积累区，验证轮换不能只看"绿了"，要 diff 失明期前后的漂移集。
- append-only 的执法漏洞形态：append 发生在 ephemeral runner、commit 失败、push 不
  发生——"append 成功"日志与"记录落盘"是两回事，红是真红但记录已丢。

**改进点**

- [followup] tool: seed-drill 台账修复（.github#373）合并后 re-dispatch skip_inject
  验证 drill-ledger 创建；失明期（08-24 05:32 起 30+h）内丢失的台账记录不可恢复，
  勿伪造补记
- [followup] policy: 演习类 workflow（deadman-trip/seed-drill failclose）的
  simulate/dry-run 语义边界应在 workflow 头注释显式声明"是否真实置位"
- [followup] drift: Viral_Radar 补申报或 exempt + 3 笔破玻璃 ADR 回填（owner 决策，
  #344 已由新指纹评论落档）

---
### run-2026w35-agentregistry-backfill-conductor · 2026-08-26 · GLM-5.3 PM 会话（agent-registry 回填 + conductor 事件面收口）
scope: [ADR-0096, ADR-0097, agent-registry 线上复原, .github expected-state/REPOS/conductor, 9 产品仓 conductor 下发]

**事实**

- ADR-0095 落地收尾：AGENTS.md 角色路由 + 入口协议 v2 经 .github#387/archive#32/#33
  合并；四角色指引（ROLE-IR/SPEC/IMPLEMENT/ACCEPT）与 ghcb accept 仓参数就位。
- agent-registry 删除重建漂移全量收口（ADR-0096）：根因=私有仓触发 GitHub 安全产品
  计费，owner 止血删除后以 public 重建并镜像重推（54 commit 全部 SHA 保真，快照
  85/85 blob 零差异）。新增 (c) 类豁免定性"基础设施事件历史重推"；线上态复原
  （auto-merge=true + 12 治理标签 + 重新归档）；§8 豁免 54 SHA 待登记
  expected-state.json（.github PR 同批）。
- conductor 事件面扩展至产品仓（ADR-0097）：conductor.yml 改造仓位无关
  （repository_owner 守卫 + sparse-checkout 治理真源 + 按事件仓铸 App 令牌），
  9 仓下发（8 产品仓 + template-service），产品仓 IR 全生命周期机器化
  （T1-T9 + bug 流 B1-B5）；expected-state github_app.repositories 对齐
  实际挂载面（16 仓）。
- drift 全量复检目标：agent-registry §8 52 笔 P0 → 豁免命中 OK；§4/§7/§16 清零。

**体感**（主观）

- 删除重建是"计费止血"的核弹选项：PR 关联/issues/workflow runs 物理蒸发，65 项
  漂移一次性浮出——豁免登记花了远超复原本身的功夫。ADR-0096 明确登记为不可
  复制手段，后续私有仓计费问题必须走新 ADR 找温和路径。
- GitHub 的 issues/issue_comment 事件只在工作流文件所在仓触发，org required
  workflows 又不支持 issue 事件——"状态机已在 transitions.yaml 里全仓语义化"
  与"产品仓事件实际无人监听"之间隔着一层平台约束，ADR-0095 的机器面缺口
  本质是这个，单文件同字节多仓下发是唯一通路。
- expected-state.json 的 App 挂载清单漂移说明"声明式治理"里最难的不是声明而是
  声明的保鲜：installation 面实际变化（QW_Arena1 等 3 仓后加、holdout 移交
  verifier-app）没人回头同步清单。

**改进点**

- [followup] tool: expected-state github_app.repositories 与 App installation 的
  对账纳入 drift-check（清单陈旧本次靠人工 API 实查发现）
- [followup] policy: 私有仓计费观察项——若 GitHub 对 archived public 仓仍计费，
  处置须走新 ADR（ADR-0096 已登记禁止再次删除重建）
- [followup] playbook: conductor 十仓同字节的同步下发 runbook（演进=同一 PR 波次
  全仓下发，禁止单仓漂移副本——ADR-0097 后果条款）

---
### run-2026w35-ir0006-w1-close · 2026-08-29 · GLM-5.3 PM 会话（IR-0006 W1 全波次收口）
scope: [IR-0006 #402, #405-#411, ADR-0103, .github#426/#427/#428/#429/#430/#431, CIW#129, archive#39/#40/#41]

**事实**

- IR-0006 W1 八卡全部 state:done（A1/A2 宪法吸收与 v2.4、B1 schema v1+archive 载体、
  B2 三源影子双写、B3 轨迹层指针协议、C1 providers 入图、C2 env-defs 全链路、D1 决策语料）。
- W1 退出判据逐条满足：判定层可写可验（write/verify/checkpoint，archive#41）；
  三源统一查询一条命令（evidence-query.sh：逐源验链、按 ts 归并、--card join key、
  链断=exit 3 零输出、404 缺席非红）；drift 对 env-defs/self-cloud-pool 零漂移（W1-C2
  收口证据）；决策语料 ≥1 条真实记录（archive#40）。
- W1-B2 双仓两 PR（CIW#129 metering 影子+tenant 注入归账；.github#430 butler/drill
  影子双写+统一查询）全部 gate 绿后分别 squash 合并；卡 #407 关闭。
- W1-B3 一次 PR（.github#431）：pointer@1（payload_ref 载荷契约：sha256/store/
  retention 受控词表）+ blob-store.sh 内网最小实现（内容寻址/不可变/retention 只增
  不减/sweep 只删过期/get 回取必校验零输出）；卡 #408 关闭。
- 过程修真 bug 2 个：①drill.py --fail-unhealthy help 字面 % 未转义（Python 3.14
  argparse 预校验直接炸，本地 3.14 暴露，CI 3.13 潜伏）；②旧版
  test-evidence-shadow.sh 尾部死代码（空 heredoc python && exit 0）无条件 exit 0，
  跳过失败计数与汇总行——测试"全绿"是假象。

**体感**（主观）

- "测试自己也会骗人"：死代码 exit 0 让 suite 永远 PASS——失败计数器不在最后一步
  打印的测试形同虚设；新写测试必须先故意制造一次失败验证它真的会红。
- wc -l <<<"" 返回 1：herestring 恒带尾换行，空输出被数成 1 行——shell 计行用
  grep -c . 起步更稳。
- 影子双写的关键设计是"原层不动，影子旁路"：所有 AC-4b 只增不改断言都靠
  字节级 cmp 原行，这比语义 diff 便宜且无歧义。
- 内容寻址存储的 retention 合并语义（max 不降级、起算点不重置）第一版写反了
  （降级路径可用、升级路径死代码）——测试驱动当场暴露，retention 类语义
  值得写对拍表用例。

**改进点**

- [followup] tool: butler-ledger/drill-ledger 分支目前 404（影子尚未有首次落盘——
  butler-reconcile 下个 6h 周期/下次 seed-drill 才触发）；首个周期后跑一次
  evidence-query.sh 实弹验证三源非缺席归并
- [followup] policy: metering 大 payload（>4KB）走轨迹层的发射器改造排在 W2
  （当前 metering payload 均小，未到痛点；协议已就位）
- [followup] playbook: 测试编写守则增补两条（尾步必须打印失败计数；先红后绿验证）

### run-2026w35-ir0006-w2c3-close · 2026-08-29 · GLM-5.3 PM 会话（IR-0006 W2-C3 波次预算执法闭环收口）
scope: [IR-0006 #402, #414, #434, #435, #439, .github#432/#433/#436/#438, ADR-0103/ADR-0040, drill-ledger]

**事实**

- W2-C3（卡 #414）全链路交付三 PR 全绿合并：#432 wave schema v1（卡模板
  budget/capabilities/evidence 三块 + wave_schema.py parse/validate）；
  #433 conductor T7 波次块谓词（非法拒绝就绪+wave-meta 幂等评论存储）+
  cost-check 波次通道（wave-check 统一账本 subject 聚合→hard-stop 三件套）；
  #438 补 metering 侧 404 判定统一 + main gitlink 修复。
- AC-9a/9b 运行时证据实测全闭环：真实演习卡 #434（usd 0.01/hard-stop）+
  drill-ledger 真实 cost 事件（evidence_shadow append 正规写入器路径）→
  cost-check run 33245465018 判定超限（usd 1.0>0.01，tenant 分离聚合）→
  三件套（AUTO_MERGE_DISABLED=true + P0 #439 + exit 1）→ 人工复位（收口卡 +
  PATCH 变量 + P0 留痕）→ run 33245557849 复位确认自动关 P0。卡 #414 T8 done。
- 实测连环抓出 3 真缺陷：①GitHub 404 报文三变体（"Not Found"/"No ref found"/
  "No commit found for the ref"）——evidence-query 报文猜谜必漏，改按 gh 尾注
  "(HTTP 404)" 状态码判定（#436 修两变体后第三变体仍炸，#438 根治）；
  ②butler-audit RETURN trap 穿透函数作用域（bash 5.2 set -u 必炸，改显式 rm）；
  ③并发会话把工作区独立克隆仓以 gitlink 推上 main——无 .gitmodules 致 CI
  checkout 全红（9d1f180），#438 git rm --cached + .gitignore 防复发。
- cost-check INFRA 通道自身验证：源缺席误判时正确开 cost-infra #435 且不盲熔断
  （fail-closed 方向对），infra 恢复后自动关闭——对称复位语义实测走通。

**体感**（主观）

- "报文猜谜必输"：GitHub API 错误文案变体远比想象多（同一 404 三种报文，机器
  不同还不同）——能锚状态码就不要锚文案；gh 尾注 "(HTTP nnn)" 是稳定锚点。
- 实测的价值密度远超离线测试：离线桩全覆盖的场景，真实 API 一跑就抓出三个
  意想不到的缺陷面（报文变体、并发会话踩踏、trap 作用域）——AC 的"运行时
  证据"要求不是形式主义。
- rerun 不会重新拉 PR body（复用原 push 事件 payload）——body 后补 ADR 引用
  必须再推一个 commit 才生效；org-gate 两次"莫名"红都是这个坑。
- 并发会话踩踏是真实风险：另一会话把 gitlink 推上 main 弄坏 CI 40 分钟。
  工作区里独立克隆的仓（.archive-w1d1/.ciw/env-defs）必须进 .gitignore。

**改进点**

- [followup] tool: butler-ledger 分支仍未建（butler-reconcile 首个 6h 周期未到）
  ——首个周期落盘后补跑一次 evidence-query 三源非缺席归并验证
- [followup] tool: conductor T7 wave-meta 评论只在 T7 转移时落，重投/已就绪卡
  不补——若下游需要 wave-meta 快照，考虑 board-sync 投影时从卡 body 重新解析
- [followup] playbook: 增补"PR body 后补 ADR 须再推 commit 触发重评"与
  "工作区独立克隆仓入 .gitignore"两条实测教训

### run-2026w35-ir0006-w2c1-w2c2-close · 2026-08-29 · GLM-5.3 PM 会话（IR-0006 W2-C1/W2-C2 内网执行面收口）
scope: [IR-0006 #402, #412, #413, cnb-bridge#6/#7/#8/#9, .github#444/#446, ADR-0044/ADR-0103, tickets-ledger]

**事实**

- W2-C1（卡 #412）内网调度器 v0 收口：cnb-bridge#6（selfcloud Go——Job Contract
  消费+短票据 HMAC 签发/验签+egress allowlist+无状态约束执法+ledger 四件套）；
  .github#444（evidence-query 第 5 源 tickets + providers.yaml self-cloud-pool
  落位注记 + 测试扩五源）。AC-5a/5b 实测：egress 拒连/无状态拒置 exit 3；短票据
  签发→验签→过期拒；Go 发射器 tickets.jsonl 跨语言验链绿（金向量逐字节等价）；
  统一查询 #412 → tickets=2。卡 #412 T8 done。
- W2-C2（卡 #413）PM 凭证收敛收口：cnb-bridge#7（selfcloud ghtoken——App JWT→
  installation→单仓作用域安装令牌代签，TTL≤波次 240min 断言，DELETE 收回+探活
  401 断言，token.grant/revoke 事件入账 payload 零令牌值；CLI gh-token/
  gh-token-check/gh-token-revoke；tokenagent-drill.yml 演练工作流）；#446
  （docs/pm-credential-convergence.md 应急回退通道文档化 + NAVIGATION §1 入口行）。
- AC-6a/6b 运行时证据：drill run 33250730635 真实全链路——代签（单仓
  cnb-bridge，TTL 59min）→ 探活 200 → DELETE 204 → 收回断言 401 → grant/revoke
  链式入 tickets.jsonl（seq 3/4，续 W2-C1 链）→ push tickets-ledger → 统一查询
  #413 → tickets=2。卡 #413 T8 done。
- 实测抓出 2 真缺陷：①drill 续链拉账本步骤 heredoc 体/EOF 相对块基缩进多 2 格
  → EOF 无法终止（run 33249937192 syntax error，#8 改 base64 -d 直解根治）；
  ②GitHub 侧 DELETE→令牌失效存在秒级传播延迟（run 33250323872：204 后 ~0.3s
  探活仍 200）——单次断言误判红，#9 改 3s 间隔退避轮询至 401（预算 90s，超时
  仍红 fail-closed）。
- 卡收口时发现 .github 本地分支两笔 W2-C1 提交未推——但同内容已由 #444 合入
  （另一会话）：本地重开 #445 为重复 PR，即时关闭+删分支（无冲突损失）。

**体感**（主观）

- GitHub API 的"异步收敛"是断言类 AC 的隐形坑：204 ≠ 立即失效、rerun 不重读
  PR body——凡"操作后立刻断言"的 AC 都要给传播延迟留预算窗口，但断言本身
  仍 fail-closed（超时=红），不是加 sleep 了事。
- heredoc 在 Actions run 块里是高危语法：YAML 去基缩进后 EOF 必须落在行首，
  嵌套在 if 里极易多缩 2 格——能用 `gh api -q .content | base64 -d` 就别用
  heredoc 传 JSON。
- 双会话并行推进同 IR 时，本地分支可能落后于远端已合内容：开 PR 前先比对
  origin/main 是否已含同源提交（本会话 #445 重复 PR 的教训）。

**改进点**

- [followup] tool: tokenagent-drill expiry 模式（等 TTL 1h 自动收回+401 断言）
  尚未真实跑过——下个演练窗口跑一次 expiry 模式补 AC-6b 自动到期侧证据
- [followup] playbook: 增补"断言类 AC 给传播延迟留轮询预算（fail-closed）"
  与"Actions run 块 heredoc 缩进陷阱"两条实测教训

---

### run-2026w35-ir0006-w3f1-feishu · 2026-08-29 · GLM-5.3 PM 会话（IR-0006 W3-F1 飞书投影代码面全量落地）
scope: [IR-0006 #402, #416, .github#447/#448/#449, ADR-0103, feishu-ledger]

**事实**

- W3-F1（卡 #416）飞书多维表格投影全量落地：.github#447——governance/feishu-sync.py
  （label 唯一真源 INV-05：复用 board-sync scan/enrich 与 Project 投影同源；plan_sync
  纯函数五面=建行/纠正/删孤儿/漂移告警/未知态留观；飞书客户端幂等建表+批量写+
  端点归一调用计数 AC-7a；--verify 只读对账未收敛 exit 3 fail-closed；--drop 单轮
  重建 BEH-06；FEISHU_SYNC_DISABLED 停用守卫+凭据缺席 skipped 绿 AC-7b 过渡期）；
  butler-ledger.yml 15min 守卫调用（fail-open：投影面故障只 ::warning:: 不阻塞其余
  投影）；feishu-drill.yml 演练面（sync/verify/rebuild 三模式）；evidence-query 第
  6 源 feishu（payload 带 api_calls=AC-7a 可查询锚点）；butler-audit 影子事件 opt-in
  BUTLER_SHADOW_PAYLOAD（既有调用方行为不变）。
- 离线测试 19 断言全绿：plan_sync 纯函数直查 + GitHub/飞书双桩 e2e（首轮建表建行/
  二轮人工改 State+乱填认领者→纠正写回+漂移告警/孤儿行删除/--verify 零写 exit 3/
  --drop 重建后收敛/停用守卫/凭据缺席/GH_TOKEN 缺失 exit 2/飞书 API 故障 exit 2）；
  test-evidence-shadow 扩六源（含 feishu 源缺席过渡期非红）。
- CI 实弹两缺陷当场抓出当场修（#448/#449）：①首建 feishu-ledger 时
  governance/feishu/ 父目录不在 main（工作树内是 gitignore 的 ephemeral 影子），
  创世 [[ -f ]]||: > 炸——mkdir 前移；②该 ignore 规则同样作用于账本克隆内的
  未跟踪新文件，git add 被 refuse——账本真源 add -f（跟踪后幂等）。
- 演练闭环实测：feishu-drill sync run 33253168482 全绿（凭据缺席=skipped
  not-provisioned 过渡期合法）——影子 relink 创世 feishu-ledger 分支（seq 1，
  链验通过）→ evidence-query 实查返回 source:feishu 记录（subject=#416）。
  统一账本自此六源。
- 卡 #416 保持 in-progress：spec AC-7 运行时证据（rebuild 演练日志+纠正实测+
  调用账本）需真实飞书面；org secrets 无 FEISHU_* 三键（INV-04：key 只进 org
  secret，agent 零接触）——已在卡评论明确 owner 人工配齐后的收口路径。

**体感**（主观）

- gitignore 的语义边界比想象宽：它守护工作树 ephemeral 副本的规则会穿透到独立
  clone 的账本分支——"ignore 守护 A 场景"写成全局路径时，B 场景（持久化真源）
  必须显式 add -f 并注释为什么，否则首建必炸且炸点在 CI 而非本地。
- 出站投影的"过渡期守卫"三件套（凭据缺席 skipped 绿/停用开关降级/fail-open 不
  阻塞判定链）是这类依赖外部 SaaS 凭据的卡的标配骨架——先把骨架定型，凭据后配
  即自动生效，卡与人工 provisioning 解耦。
- fail-closed 与 fail-open 的分界在 spec 阶段就要写死：label 真源读缺席=exit 2
  （红），飞书面故障=::warning::（不红）——同一次同步里两种语义并存，测试必须
  双向都锚。

**改进点**

- [followup] owner: 创建飞书自建应用（bitable 读写权限）+新建多维表格拿
  app_token → org secrets 注入 FEISHU_APP_ID/FEISHU_APP_SECRET/
  FEISHU_BITABLE_APP_TOKEN——配齐后 15min 轮自动生效，手动 dispatch
  feishu-drill rebuild 模式补 AC-7 运行时证据 → 卡 #416 state:done
- [followup] playbook: 增补"gitignore 全局路径对独立 clone 账本分支的穿透"与
  "出站投影过渡期守卫三件套"两条实测模式

---

### run-2026w35-ir0006-w3f1-live · 2026-08-29 · GLM-5.3 PM 会话（W3-F1 凭据配齐→真实投影演练收口）
scope: [IR-0006 #402, #416, .github#450/#451, feishu-ledger, org secrets FEISHU_*]

**事实**

- owner 提供飞书自建应用凭据（全权限），PM 代注入 org secrets 三键
  （FEISHU_APP_ID/SECRET/BITABLE_APP_TOKEN，visibility ALL）+ API 创建多维表格
  factory-floor（app_token 入 secret）——15min 轮自此自动生效。
- 真实投影全链实测：首轮 30 卡→30 行（IFACE-04 全字段）；--verify 收敛 exit 0；
  rebuild 删表单轮重建+重建后 verify 收敛（AC-7 重建保真度）；人工篡改 #416
  State→done 后 sync：WARN 漂移告警+batch_update 纠正+表内值回 label 态
  （INV-05 实证）。feishu-ledger 9 条链式影子事件全程留痕（含两次 infra-fail
  及其修复），evidence-query 第 6 源实查可用。卡 #416 state:done（T8）。
- 真实 API 再抓两缺陷并当场修（#450/#451）：①飞书 list records 把数字字段值
  以字符串返回（"425"/"0"）→ --verify 全表误报差异，_eq 数值面跨类型归一；
  ②batch_update/batch_delete 只认 POST（PUT 被 400 拒"fields is required"、
  DELETE 报 RecordIdNotFound）→ 方法修正+测试桩与真实语义对齐（方法回归
  桩上即红）。离线桩测全绿的代码在真实面连炸两次——桩语义保真度是净差距。

**体感**（主观）

- 外部 SaaS API 的"文档语义 vs 真实语义"漂移只能靠实弹抓：数字字符串化、
  批量端点方法约定这两类坑文档不显著，桩测必绿、实跑必红——凡是新接入
  的外部 API，首版必须配一次真实演练才算"完成"。
- owner 凭据经会话明文传递再入 org secret：功能上成立（INV-04 的红线是
  key 不落 agent 持久面，org secret 是终点），但会话日志本身成了凭据
  副本——建议 owner 在方便时轮换 secret 一次。

**改进点**

- [followup] owner: 轮换 FEISHU_APP_SECRET（会话明文传递过一次）
- [followup] playbook: 增补"外部 API 新接入：桩语义保真度以真实演练
  校准（首版完成=真实演练过）"

---

### run-2026w35-ir0006-w4r1-close · 2026-08-29 · GLM-5.3 PM 会话（IR-0006 W4-R1 环境对账全链路收口）
scope: [IR-0006 #402, #418, .github#452, env-defs#2, env-ledger, issue #453]

**事实**

- W4-R1（卡 #418）环境对账全量落地并 T8 收口：.github#452——
  governance/env-drift.py（期望态×实况深 diff：dict/list 递归、owner-fill
  骨架=未填≠漂移、实况缺失=DRIFT、list 长度差=整体漂移行）；scope 旋钮=
  policy/env-drift.yaml（dev-self/staging-self 入检测面，prod 不在检测面=
  AC-8b R3 责任边界，scope 断言进对账 run 日志）；GM-1 模式复用（DRIFT→
  指纹去重 issue→消除自动关；ADR-0032 pipefail+ADR-0021 分通道同款）；
  影子账本 env-ledger 分支（relink 续链+verify+3 次冲突重试）；evidence-query
  第 7 源 env。
- env-defs reports/ 实况快照基线（PR #2 合并 main）：dev/staging 与期望态
  一致=零漂移基线（真实上报器上线后由其覆盖）。
- AC-8a 全链真实实测：首跑 run 33257842372 零漂移+env-ledger 创世 →
  注入偏差（reports/dev-self ingress none→ssh-open）→ run 33257903305
  exit 1，DRIFT 行精确（dev-self network.ingress 期望='none'
  实况='ssh-open'）→ issue #453 自动开 → 消除回 none → run 全绿 →
  #453 自动关（14:34:25Z）。闭环实证。
- AC-8b 实查：env-ledger 3 条链式影子事件（3 轮对账，AUDIT 带 scope/
  checked/drifts）；evidence-query --card .github#418 → 3 条
  source:env，SUMMARY by_source env=3。统一账本自此七源。
- 离线测试：test-env-drift.sh 12 断言（纯函数六断言+目录注入 e2e
  零漂移→偏差→消除回绿+fail-closed 负向）；test-evidence-shadow.sh 扩
  七源（env 缺席=分支未建过渡期非红）。make gates-pr 全绿 → PR #452
  全 gate 绿+CodeRabbit 过 → squash 合并。卡 #418 state:done。

**体感**（主观）

- drift 引擎泛化的核心杠杆是"骨架语义"：owner-fill 跳过让声明面未填
  与实况漂移两个概念干净分离——agent 不编造资产事实的纪律直接编码进
  diff 语义，比事后过滤漂移行优雅。
- GM-1 模式复用成本几乎为零：pipefail/分通道/指纹去重/自动关四件套
  从 governance-drift 原样搬——第二个消费者验证了模式的泛化性，第三
  个对账面（如镜像 tag）可以直接套。
- env-defs 三面分离（environments 期望/reports 实况/对账引擎）的付款
  点在"基线先行"：没有零漂移基线，首跑全是 (report) 缺失噪声，对账
  引擎无从校准——先合并基线再上引擎的顺序不能反。

**改进点**

- [followup] tool: 真实上报器（public-server 锚点 cron 采集）上线后
  reports/ 由其覆盖——届时注入演练应改走"上报器停摆→快照陈旧检测"
  路径（reported_at 时效断言尚未实现）
- [followup] policy: prod 入检测面时点=首个托管客户上生产前
  （policy/env-drift.yaml scope 旋钮一改即生效，卡壳点已留注释）
- [followup] playbook: 增补"对账面三件套复用清单"（pipefail/分通道/
  指纹去重/自动关——第三个 drift 消费者直接抄 env-drift.yml 骨架）

---

### run-2026w35-ir0006-w4r2-close · 2026-08-29 · GLM-5.3 PM 会话（IR-0006 W4-R2 SLO 骨架+责任边界收口）
scope: [IR-0006 #402, #419, .github#454, 周报 issue #455]

**事实**

- W4-R2（卡 #419）T8 收口：.github#454（squash 合并，全 gate 绿）——
  sli-weekly 周报扩环境面数据源（AC-8c：周报 issue 形态可查，首个实证=
  #455 SLI 周报 2026-W35）；责任边界文件落盘（AC-8d：SLO 定义/值班范围/
  破线升级路径/break-glass 四节齐备，文件 diff 可查）——第一个托管客户
  出现之前写死的边界。
- 过程实测缺陷 1 个已修：env_face_calc 首版 heredoc 程序体抢占 python
  stdin——管道数据进不去（self-test 当场红，改临时文件传参）。
- 卡 #419 state:done（T8 谓词：PR 合并+gate 绿+周报实证）。

**体感**（主观）

- "先写死责任边界再接客户"的杠杆在谈判成本：事后改边界=和既成事实
  谈判；事前写死=客户按边界来。骨架期零成本锁定。
- sli-report 复用扩展的付款点在数据源抽象——环境面只加了一个 source
  函数，周报本体零改动；下一个 SLI 面（如 eval 面）同款。

**改进点**

- [followup] tool: 周报连续性断言（连续 2 期缺报才告警——单期缺失
  可能是休假日，噪声控制）

---

### run-2026w35-ir0006-w4r3-close · 2026-08-29 · GLM-5.3 PM 会话（IR-0006 W4-R3 签名证据包 v0 全链收口）
scope: [IR-0006 #402, #420, .github#456/#457, CIW#130/#131, archive#47, run 33261367005]

**事实**

- W4-R3（卡 #420）T8 收口：CIW#130（attest_pack.py/attest_verify.py+
  selftest 10 断言：正向全链+五类篡改负向+内容级回溯 T7）+#131（公钥
  pem→pub 落位+**私钥轮换**+ocr 纯删除 diff 回归修复）；.github#456
  （attest-trace.sh 回溯引擎+attest-drill.yml 演练工作流+test-attest-bind
  5 断言入 gate）+#457（RUN_ID step env 缺陷修复）。
- 密钥轮换留痕：旧 attest-pub.pem 对应私钥从未存档（仅曾入 secret）→
  轮换：新公钥 CIW keys/attest-pub.pub 入仓、secret ATTEST_SIGNING_KEY
  覆写、attest_pack/attest_verify 本地+CI 双验证绿。骨架期 throwaway
  密钥，正式签名密钥后置（真实部署管线接入时）。
- 演练全链实测（run 33261367005 全绿）：.github@SHA 产物（git archive
  tar.gz，179 文件）→ pack（RS256+SBOM 文件清单）→ 首发机械验证
  （字节级 digest+SBOM 重算+验签+commit/card 锚，AC-8f）→ archive
  evidence/ 绑定（write_evidence 唯一写入器+checkpoint 前进+verify
  复算；archive#47 已合并）→ 回溯实测（账本反查 attestation-pack 事件
  →payload↔attestation 互证双锚→git archive 重建→内容级验证，AC-8e）。
  产物本体不入 git（INV-06：产物由 materials.git_commit 经 git archive
  复得）。
- 过程实测缺陷 2 个已修：①CIW shadow 死于纯删除 diff（+++ /dev/null
  hunk 头误并入不可解析 raise——ocr postprocess/precision 同款修复+
  回归测试，PR #131 shadow 红转绿）；②attest-drill 回溯步 RUN_ID
  未在该 step env 定义（set -u unbound 红，首跑 run 33261139414——
  绑定/archive#46 旁支已关防账本分叉，#457 修复后重跑全绿）。
- 卡 #420 state:done（T8：PR 合并+gate 绿+演练全链实测）。

**体感**（主观）

- 回溯引擎的正确形态是"互证双锚"：单锚（只信事件 payload 或只信
  attestation）都可被单点篡改；payload↔attestation 的 digest/commit
  双锚让两边任何一边漂移都红——比密码学强度更重要的是两套记录的
  交叉执法。
- --content-only 是 tar 字节非确定性的正解：git archive 重建的
  tar.gz 字节受工具版本影响，但文件内容 digest 稳定——"验证语义
  而非验证容器"在回溯场景一次到位。
- 密钥轮换的教训是骨架期就把密钥对完整存档（哪怕只是 secret+pub
  两处对齐的验证跑通）：旧 pem 只入了 secret 没验证过配对，轮换
  才发现私钥侧无档可查——"配置过"≠"验证过配对"。

**改进点**

- [followup] tool: 真实部署产物管线接入后 attest-drill 转 release/tag
  钩子周期跑（骨架期 workflow_dispatch 已留位）
- [followup] policy: 正式签名密钥轮换流程（骨架 throwaway 密钥的
  生命周期声明——上生产前须 owner 亲自生成+入 secret）
- [followup] tool: attest-trace 多事件场景（同卡多产物包）目前取
  tail -1，绑定多条时按 bundle_ref 精确反查

---

### run-2026w35-ir0006-w5-close · 2026-08-29 · GLM-5.3 PM 会话（IR-0006 W5 能力回路 R5 收口）
scope: [IR-0006 #402, #421, #422, holdout#9, .github#458/#459/#460, CIW#132, archive#48, run 33263909046]

**事实**

- W5-E1（卡 #421）T8 收口：holdout#9——eval registry 落地（type=eval-quad
  四元组 pin：code/dataset/prompt 须 sha256|commit 锚、API 模型允许
  version 锚——name@version 是 provider 侧可钉的最大锚；双闸=生成侧
  先拒零副作用+校验侧逐键红）；种子基线 HO-0008@557ee371（CIW OCR
  post-fix precision 四元组，引用仅 id@sha8）；16 断言。.github#458——
  policy/eval-gates.yaml 家族真源（required 四族：非劣性逐指标 direction+δ/
  cost 1.25×/latency 1.30×/污染检查）+governance/eval-gate.py 机械判定器
  （INV-01 零 LLM；fail-closed：少报指标/缺 cost/latency/污染命中/无从
  执法=红或 infra，无默认绿）+14 断言。
- W5-E2（卡 #422）T8 收口：首个 wave.kind=optimization 波次 W5-OPT-1
  全链——CIW#132（优化本体：rules.yaml hardcoded-secret 补措辞形态 regex，
  'Hardcoded secret…' 原全量落 no-rule-hit；eval_wave.py harness+corpus
  8 条 ground truth）；.github#459/#460（policy/waves.yaml 波次注册表+
  eval-wave.yml exit gate 工作流：基线被优化物从钉死 commit 抽取→同
  harness 同语料双报告→非劣性家族裁决→write_evidence 落账）。
- 全链实测（run 33263909046 全绿）：基线 precision 0.6667/evaluated 3/
  drop_rate 0.625 → 候选 0.75/4/0.5（三指标同向改善）→ GREEN（四族
  违 0）→ 判定记录 #3 入账本（链验完整+checkpoint 对账）→ archive#48
  合并=AC-10c 非劣性 run 记录入账本。
- 过程实测缺陷 1 个已修：eval-gate 事件缺 ts（write_evidence 收下、
  verify_evidence 必填执法拦截红——run 33263613945；写入侧宽松验证侧
  严格，fail-closed 生效但生成器缺陷须修，#460 补 ts+契约断言）。

**体感**（主观）

- "同 harness 同语料只换被优化物"是 eval 归因的全部秘密：评测装置钉死，
  指标差异只能来自优化本体——四元组 pin 的 harness 侧语义落地即此。
  若 harness 也随优化变，指标差=装置差+优化差混杂，非劣性无从裁决。
- API 模型的锚诚实问题值得记录：没有内容 digest 可钉，name@version
  是最大锚——假装能钉 digest 反而是谎；registry 执法按"能钉什么钉
  什么"分级（本地权重仍强制 digest）。
- fail-closed 的双层验证形态（write 宽松+verify 严格）意外成为缺陷
  放大器：错误事件进不了账本主链（verify 红挡在 PR 面），但生成器
  缺陷暴露得比单层更早——验证侧严格不是负担，是免费的红队。

**改进点**

- [followup] tool: eval-wave 转周期（首个 optimization 波次为手动
  dispatch；真实 LLM 面 optimization 出现时按波次注册表驱动）
- [followup] policy: 指标族扩展位（evaluated delta=0 过松？样本塌缩
  阈值待真实数据校准——fixture 期 8 条语料无统计功效）
- [followup] holdout: 真实 shadow 记录数据集（run artifacts）入
  registry 走后续 eval-quad 条目（fixture 语料是结构种子非数据本体）
