# agent-tools（L2，2026-08-25 退役→GitHub 归档）

TS 工具服务器（bash/read_file/write_file/web_search/gitcode-pr），被
agent-registry registry/tools/*.yaml 的 implementation 字段引用（ADR-0021）。

退役：ADR-0085 决策 2——新范式工具=GitHub workflow+脚本+ghcb，无运行时消费。
git 历史即存档：https://github.com/Cloudbird-Software/agent-tools（archived，只读）。
