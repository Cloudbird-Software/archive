# agent-platform（L2，2026-08-25 退役→GitHub 归档）

渲染 agent-registry 声明到 openjiuwen/jiuwenswarm 运行时的执行层仓（ADR-0025）：
渲染器/SwarmFlow workflow 编译器/机制原型执行面/TUI 可观测+agentctl。

退役：ADR-0085 决策 2——多 agent 编排范式放弃，GitHub SSOT+Actions 内角色替代。
内容不再快照（约 200 文件的 Python 工程），git 历史即存档：
https://github.com/Cloudbird-Software/agent-platform（archived，只读）。
